
// string inbuilt functions

#include <iostream>
#include <algorithm> // It is required library to use reverse function


using namespace std;

int main(void)
{

    // Intial string 

    string a = "Hello world" ;

    // // push_back() -> It will add one character in strings ending point 
    // a.push_back('a');
    // // It will give output "Hello worlda"


    // // pop_back() -> It will remove last one character in strings ending point
    // a.pop_back();
    // // It will give output "hello worl"


    // // append("text") -> It will add the text that you wrote in brackets
    // a.append(" my name is ansh chovatiya");
    // // It will give output "Hello world my name is ansh chovatiya"

    // // clear() -> It will delete the elments in string and make it a empty string
    // a.clear();
    // // It will return nothing

    // // + operator -> It will do exectly what append does 
    // a = a + " ansh chovatiya"; // It will give output "hello world ansh chovatiya"
    // a = " ansh chovatiya " + a;  // It will give output "ansh chovatiya hello world"

    // reverse() -> It will reverse the string 
    // reverse(a.begin(),a.end()); // It will give output "dlrow olleH";

    // // to_string() -> It will chage number into string 
    // int x = 12345;
    // string s = to_string(x);
    // // now 12345 is a string you can access it's elements by s[0] or s[1]

    // // stoi() -> It will chage string into number
    // string x = "12345";
    // int s = stoi(x); // now 12345 is decimal number that is stored in s variable




    cout << a << endl;
    // cout << s << endl;
     
}